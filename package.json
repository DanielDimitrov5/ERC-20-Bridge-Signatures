{
  "name": "erc20-bridge-backend",
  "version": "1.0.0",
  "description": "ERC20 Bridge backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prisma": "^5.7.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "5.7.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.1"
  }
}
